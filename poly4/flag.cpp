#include <vector>
#include "flag.h"
typedef vector<flag> flaglist;
flag::flag(bool inC, bool inR, bool inL, bool inB, bool inT, bool inF, bool inS): C(inC), R(inR), L(inL), B(inB), T(inT), F(inF), S(inS)
{
}
flag::~flag()
{
}